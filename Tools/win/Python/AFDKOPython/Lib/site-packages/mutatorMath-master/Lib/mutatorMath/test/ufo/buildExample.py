# -*- coding: utf-8 -*-

""" Example of a build script for a specific mutatorMath project.
	A script like this could live near the designspace description.
	It could be called to build a bunch of instances without opening any application. 

"""
from mutatorMath import build
import os, glob

path = os.path.join(os.getcwd(), "*.designspace")
version = 2
for ds in glob.glob(path):
    print "\tbuilding", ds
    build(documentPath=ds, outputUFOFormatVersion=version)
print 'done'